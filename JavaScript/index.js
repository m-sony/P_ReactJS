function getShortMessage(message) {
    var array = [];

    message.map((bt) => {
        if(message.size < 50)
        {
            array.prototype.push(message);
        }
    })

    return array;
}